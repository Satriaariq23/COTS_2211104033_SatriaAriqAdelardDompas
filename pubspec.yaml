name: cots_2211104033
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.6.6
  assets: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/onboardi1.png
    - assets/images/onboardi2.png
    - assets/images/onboardi3.png
    - assets/images/Gojek_Logo.png
    - assets/images/Language.png
    - assets/images/GoCar.png
    - assets/images/GoFood.png
    - assets/images/GoRide.png
    - assets/images/GoSend.png
